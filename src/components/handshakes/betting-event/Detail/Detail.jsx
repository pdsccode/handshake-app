import React from 'react';
// import PropTypes from 'prop-types';
// import { connect } from 'react-redux';

class DetailBettingEvent extends React.Component {
  static propTypes = {
    // children: PropTypes.any.isRequired,
  }

  // constructor(props) {
  //   super(props);
  //   // ...
  // }

  render() {
    return (
      <div>
        detail betting event
      </div>
    );
  }
}

// const mapState = (state) => {
//   const { auth } = state;
//   return { auth };
// };

export default DetailBettingEvent;

// export default connect(mapState)(NewComponent);
